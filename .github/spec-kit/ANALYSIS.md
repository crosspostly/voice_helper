# 🔍 Analysis — Анализ спецификаций и задач

## Выявленные конфликты

### 1. localStorage vs Performance
**Проблема**: Синхронные операции записи в localStorage блокируют UI.  
**Решение**: Внедрить debounce для `usePersistentState` или переход на IndexedDB.

### 2. WebSocket lifecycle vs Audio stream
**Проблема**: Микрофон продолжает отправлять аудио после закрытия WebSocket.  
**Решение**: ✅ Исправлено в последнем коммите (guard на `ws.readyState`).

### 3. Auto-reconnect vs Manual stop
**Проблема**: Auto-reconnect срабатывает даже после намеренной остановки пользователем.  
**Решение**: Добавить флаг `isSessionActiveRef` для отслеживания намерения пользователя.

### 4. Tailwind цвета vs custom palette
**Проблема**: Tailwind классы `bg-gray-700` конфликтуют с бежевой палитрой.  
**Решение**: Использовать `bg-[#FDF6ED]` или расширить Tailwind config кастомными цветами.

### 5. Preset prompts vs Multilingual UI
**Проблема**: Промпты персон на английском, но UI на русском — может быть confusion.  
**Решение**: Добавить русские версии промптов или пояснительный текст.

## Потенциальные проблемы

### Security
- **API key в localStorage** — уязвимость XSS. Решение: предупреждение для пользователей.
- **Content Safety disabled в 18+ mode** — риск токсичного контента. Решение: явное предупреждение.

### Performance
- **Большие transcript в localStorage** — может вызвать slowdown. Решение: лимит на количество сообщений или compression.
- **Keep-alive каждые 20 секунд** — избыточный трафик? Решение: увеличить до 30-40 сек.

### UX
- **Пустой dropdown персон** — если не загрузились. Решение: ✅ Добавлены fallback тексты.
- **Модалки не читаемые** — низкий контраст. Решение: ✅ В процессе исправления (Phase 2).

## Рекомендации

1. **Приоритизировать Phase 2** (UX улучшения) — критично для пользовательского опыта.
2. **Провести аудит безопасности** перед публичным релизом.
3. **Добавить error boundaries** для React компонентов.
4. **Внедрить telemetry** (опционально) для отслеживания ошибок в продакшене.
